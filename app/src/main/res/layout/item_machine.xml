<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="1dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_machines"
            app:tint="@color/washapp_primary"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/machineLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:textAppearance="@style/TextAppearance.WashApp.Title"
            tools:text="Machine 1 · Available" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/toggleAvailabilityButton"
            style="@style/Widget.WashApp.Button.Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/action_toggle_availability" />

        <ImageButton
            android:id="@+id/deleteMachineButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_delete_machine"
            android:padding="8dp"
            android:src="@drawable/ic_delete"
            app:tint="@color/washapp_error" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
